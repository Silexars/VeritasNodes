#pragma once

#include <Veritas/Nodes/Node.h>

namespace Veritas {
    namespace Nodes {
        class Flux {
            public:
                void flow(Node& node);
            private:
        };
    }
}
